#include <stdbool.h>
#include <stdio.h>



int main(int argc, char** argv){
    int numberToTest = 0;
    int result = numberToTest % 2;
    bool hasModule = (result > 0);

    if(hasModule){
        return result;
} else{
    printf("Number")
}

}